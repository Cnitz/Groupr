define(['Application', 'angular', 'ngMocks'], function(app, ng, mocks) {
    describe('testing folder controller', function() {
        beforeEach(module('Groupr'));

       var $controller;

        beforeEach(inject(function(_$controller_) {
            $controller = _$controller_;
        }));

        describe('', function() {

        });
    });
});